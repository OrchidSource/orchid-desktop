import { OrcuiCountrySelectorDirective } from './orcui-country-selector.directive';

describe('OrcuiCountrySelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrcuiCountrySelectorDirective();
    expect(directive).toBeTruthy();
  });
});
